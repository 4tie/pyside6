# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Baseline Backtest Crash Paths
  - **CRITICAL**: This test MUST FAIL on unfixed code — failure confirms the bugs exist
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior — it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate each crash before the fix is applied
  - **Scoped PBT Approach**: Scope the property to the concrete failing call paths in `_run_baseline_backtest` and `_on_baseline_backtest_finished`
  - Create `tests/test_strategy_lab_bugfix_exploration.py`
  - Test Bug 1 — `prepare_sandbox` type error: call `prepare_sandbox(AppSettings(...), "MyStrategy")` directly and assert `FileNotFoundError` or `AttributeError` is raised (wrong arg types on unfixed code)
  - Test Bug 2 — missing export flags: capture the command list produced by `build_backtest_command` without `extra_flags` and assert `"--backtest-directory"` is absent from `cmd.as_list()` on unfixed code
  - Test Bug 3a — `parse_backtest_results` AttributeError: call `improve_service.parse_backtest_results(some_dir)` and assert `AttributeError` is raised (method does not exist on unfixed code)
  - Test Bug 3c — missing callbacks: inspect the `execute_command` call kwargs and assert `on_output` is bound to `self._on_process_stdout` (non-existent attribute) on unfixed code
  - Test Bug 4/5 — duplicate method resolution: use `inspect.getsourcelines` to confirm Python resolves `LoopPage._restore_preferences` and `LoopService.run_gate_sequence` to the later (canonical) definitions, confirming the earlier copies are dead
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests FAIL (this is correct — it proves the bugs exist)
  - Document counterexamples found:
    - `TypeError` or `FileNotFoundError: Strategy file not found: .../AppSettings object.py`
    - `AttributeError: 'ImproveService' object has no attribute 'parse_backtest_results'`
    - `AttributeError: 'LoopPage' object has no attribute '_on_process_stdout'`
    - `"--backtest-directory"` absent from baseline command
  - Mark task complete when tests are written, run, and failures are documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Gate Backtests and Non-Baseline Paths Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Create `tests/test_strategy_lab_bugfix_preservation.py`
  - Observe on UNFIXED code: `_start_gate_backtest()` passes `self._terminal.append_output` / `self._terminal.append_error` as subprocess callbacks — record this as the baseline behavior
  - Observe on UNFIXED code: `_run_next_iteration()` calls `prepare_sandbox(config.strategy, iteration.params_after)` — record arg types (str, dict)
  - Observe on UNFIXED code: `_parse_current_gate_results()` calls `parse_candidate_run(self._current_gate_export_dir, self._gate_run_started_at)` — record arg types (Path, float)
  - Observe on UNFIXED code: canonical `run_gate_sequence` (second definition) calls `build_in_sample_gate_result` and `evaluate_gate1_hard_filters` — record this as the baseline behavior
  - Write property-based test (using `hypothesis`): for any `LoopConfig` with valid strategy/timerange, the gate backtest command always routes output to `terminal.append_output` / `terminal.append_error` (not to any non-existent callback)
  - Write property-based test: for any `(strategy_name: str, params_after: dict)` pair, `_run_next_iteration` always calls `prepare_sandbox` with a `str` first arg and a `dict` second arg
  - Write property-based test: for any `LoopConfig`, the canonical `run_gate_sequence` always calls `build_in_sample_gate_result` (not the stale direct `GateResult(...)` construction)
  - Write unit test: `_restore_preferences` and `_save_preferences` round-trip — call save then restore and assert the same field values are recovered
  - Verify all tests PASS on UNFIXED code (confirms baseline behavior to preserve)
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [x] 3. Fix all five bugs in loop_page.py and loop_service.py

  - [x] 3.1 Fix `prepare_sandbox()` contract mismatch in `_run_baseline_backtest`
    - File: `app/ui/pages/loop_page.py`, method `_run_baseline_backtest`
    - Change `self._improve_service.prepare_sandbox(settings, strategy)` → `self._improve_service.prepare_sandbox(strategy, {})`
    - `strategy` is already the strategy name string; `{}` is the correct `candidate_config` for a baseline run (copies the unmodified `.py` file with a minimal params file)
    - _Bug_Condition: `isBugCondition_1(call)` — `call.prepare_sandbox.arg0 IS AppSettings` AND `call.prepare_sandbox.arg1 IS str`_
    - _Expected_Behavior: `prepare_sandbox(strategy_name: str, candidate_config: dict)` receives correct types; sandbox created without exception_
    - _Preservation: `_run_next_iteration` call to `prepare_sandbox(config.strategy, iteration.params_after)` is untouched_
    - _Requirements: 2.2, 3.3_

  - [x] 3.2 Add `--strategy-path` and `--backtest-directory` flags to baseline command
    - File: `app/ui/pages/loop_page.py`, method `_run_baseline_backtest`
    - Build `extra_flags = ["--strategy-path", str(sandbox_dir), "--backtest-directory", str(export_dir)]` before calling `build_backtest_command`
    - Pass `extra_flags=extra_flags` to `build_backtest_command(...)`, mirroring the pattern in `_start_gate_backtest`
    - `sandbox_dir` is already computed earlier in the method (after fix 3.1); `export_dir` is already computed as `sandbox_dir / "baseline_export"`
    - _Bug_Condition: `isBugCondition_2(cmd)` — `"--backtest-directory" NOT IN cmd.as_list()` OR `"--strategy-path" NOT IN cmd.as_list()`_
    - _Expected_Behavior: Freqtrade writes result zip to `export_dir`; `parse_candidate_run(export_dir, ...)` finds the zip_
    - _Preservation: `_start_gate_backtest` command building is untouched_
    - _Requirements: 2.4, 3.4_

  - [x] 3.3 Fix non-existent method call, missing callbacks, and record `started_at`
    - File: `app/ui/pages/loop_page.py`, methods `_run_baseline_backtest` and `_on_baseline_backtest_finished`
    - Change 3a — Fix subprocess callbacks in `_run_baseline_backtest`: replace `on_output=self._on_process_stdout, on_error=self._on_process_stderr` with `on_output=self._terminal.append_output, on_error=self._terminal.append_error`
    - Change 3b — Record start timestamp: add `self._baseline_run_started_at = time.time()` immediately before `self._process_service.execute_command(...)` in `_run_baseline_backtest`
    - Change 3c — Fix parse call in `_on_baseline_backtest_finished`: replace `self._improve_service.parse_backtest_results(export_dir)` with `self._improve_service.parse_candidate_run(export_dir, self._baseline_run_started_at)`
    - Initialize `self._baseline_run_started_at = 0.0` in `_reset_iteration_runtime()` or `_ensure_loop_runtime_state()` alongside the other `_*_started_at` fields
    - _Bug_Condition: `isBugCondition_3(call)` — `call.method_name == "parse_backtest_results"` AND `"parse_backtest_results" NOT IN dir(ImproveService)`_
    - _Expected_Behavior: `execute_command` receives `terminal.append_output/append_error`; `parse_candidate_run(export_dir, started_at)` returns `BacktestResults` without exception_
    - _Preservation: `_start_gate_backtest` callbacks and `_parse_current_gate_results` parse call are untouched_
    - _Requirements: 2.1, 2.3, 3.2, 3.4_

  - [x] 3.4 Reset `_latest_diagnosis_input` at the start of `_on_start()`
    - File: `app/ui/pages/loop_page.py`, method `_on_start`
    - Add `self._latest_diagnosis_input = None` at the very start of `_on_start()`, before the baseline check (`needs_baseline = self._latest_diagnosis_input is None`)
    - This ensures every new session always runs a fresh baseline backtest regardless of any previous session's state
    - _Bug_Condition: `_latest_diagnosis_input` holds stale data from a prior session; `needs_baseline` evaluates to `False`; baseline skipped_
    - _Expected_Behavior: `_latest_diagnosis_input is None` at the start of every new session; baseline always runs_
    - _Preservation: Within a running session, `_latest_diagnosis_input` is still populated by a completed gate and reused across iterations without re-running the baseline_
    - _Requirements: 2.5, 3.1_

  - [x] 3.5 Delete dead duplicate code in `loop_page.py`
    - File: `app/ui/pages/loop_page.py`
    - Delete the entire `_build_config_panel` method body (lines ~359–551) — `_init_ui` calls `_build_config_group` instead; `_build_config_panel` has zero callers
    - Delete the stale first definitions of the following methods (the earlier occurrences shadowed by later canonical ones):
      - `_restore_preferences` (old, lines ~714–752)
      - `_save_preferences` (old, lines ~753–792)
      - `_on_timerange_preset` (old, lines ~793–800)
      - `_on_iteration_mode_changed` (old, lines ~822–823)
      - `_update_stat_cards` (old, lines ~943–964)
      - `_clear_history_ui` (old, lines ~911–924)
    - Do NOT delete `_on_select_pairs` or `_on_validation_mode_changed` — these appear only once
    - After deletion, verify the canonical definitions (lines ~1352+) are intact and that `_init_ui` / `showEvent` / `_check_config_guard` still reference the correct method names
    - _Bug_Condition: `isBugCondition_4(class_body)` — `"_build_config_panel" IN class_body.methods` AND `"_build_config_panel" NOT IN callers(class_body)` OR `count(method_name, class_body) > 1`_
    - _Expected_Behavior: Each method name appears exactly once; no unreachable code; no risk of accidental widget overwrite_
    - _Preservation: `_build_config_group()` and all widgets it creates are untouched; canonical method implementations are untouched_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [x] 3.6 Delete stale `run_gate_sequence` in `loop_service.py`
    - File: `app/core/services/loop_service.py`
    - Delete the first `run_gate_sequence` definition (lines ~1001–1097) — the stale copy that lacks hard-filter evaluation and uses direct `GateResult(...)` construction
    - The canonical second definition (lines ~1914+) calls `build_in_sample_gate_result`, `evaluate_gate1_hard_filters`, `_mark_hard_filter_rejection`, `_mark_gate_failure`, `evaluate_post_gate_hard_filters`, and sets `iteration.status = "success"` on pass — keep it unchanged
    - After deletion, confirm `_build_hyperopt_command` immediately follows `record_iteration_error` in the file
    - _Bug_Condition: `isBugCondition_5(class_body)` — `count("run_gate_sequence", class_body.methods) == 2`_
    - _Expected_Behavior: Exactly one `run_gate_sequence` definition; canonical behavior preserved; file is ~97 lines shorter_
    - _Preservation: The canonical `run_gate_sequence` (second definition) is not modified in any way_
    - _Requirements: 3.1, 3.4, 3.5_

  - [x] 3.7 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Baseline Backtest Crash Paths
    - **IMPORTANT**: Re-run the SAME tests from task 1 — do NOT write new tests
    - The tests from task 1 encode the expected behavior (no crashes, correct arg types, correct method names, correct callbacks)
    - Run `tests/test_strategy_lab_bugfix_exploration.py` against the fixed code
    - **EXPECTED OUTCOME**: Tests PASS (confirms all five bugs are fixed)
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [x] 3.8 Verify preservation tests still pass
    - **Property 2: Preservation** - Gate Backtests and Non-Baseline Paths Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 — do NOT write new tests
    - Run `tests/test_strategy_lab_bugfix_preservation.py` against the fixed code
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions in gate backtest flow, iteration flow, or preferences persistence)
    - Confirm all tests still pass after fix (no regressions)

- [x] 4. Write additional unit and integration tests
  - Create `tests/test_strategy_lab_bugfix_unit.py`
  - Unit test `_run_baseline_backtest` with mocked services: assert `prepare_sandbox` is called with `(strategy_name: str, {})`, assert `cmd.as_list()` contains `"--strategy-path"` and `"--backtest-directory"`, assert `execute_command` is called with `on_output=terminal.append_output` and `on_error=terminal.append_error`
  - Unit test `_on_baseline_backtest_finished` with mocked `parse_candidate_run`: assert it is called with `(export_dir, baseline_run_started_at)` where `baseline_run_started_at > 0`, and that `_latest_diagnosis_input` is populated on success
  - Unit test `_on_baseline_backtest_finished` with `exit_code != 0`: assert no parse call is made and `_baseline_in_progress` is reset to `False`
  - Unit test that `LoopPage` has no `_build_config_panel` method after fix 3.5 (use `hasattr`)
  - Unit test that `LoopService.run_gate_sequence` calls `build_in_sample_gate_result` (not the stale direct `GateResult(...)` construction) after fix 3.6
  - Unit test session reset: start a second loop session after a completed first session and assert `_latest_diagnosis_input` is `None` at the start of `_on_start`
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 3.4_

- [x] 5. Checkpoint — Ensure all tests pass
  - Run the full test suite: `pytest tests/test_strategy_lab_bugfix_exploration.py tests/test_strategy_lab_bugfix_preservation.py tests/test_strategy_lab_bugfix_unit.py --tb=short`
  - Ensure all tests pass; ask the user if questions arise
  - Run `ruff check app/ui/pages/loop_page.py app/core/services/loop_service.py` to confirm no lint errors introduced by the deletions
  - Confirm `_build_config_group` and all canonical method definitions are intact in `loop_page.py`
  - Confirm the canonical `run_gate_sequence` is intact in `loop_service.py`
