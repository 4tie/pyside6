[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short -q
markers =
    bug_condition: marks tests that encode bug conditions (expected to fail on unfixed code)
    preservation: marks tests that verify first-call behavior is unchanged by the fix (must pass on unfixed code)
