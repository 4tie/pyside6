# Tech Rules

- Python + PySide6 desktop app
- Freqtrade runs as subprocess/module integration
- Core app must remain useful without any live AI provider
- AI/provider integration should be optional later, not a hard dependency now
- Use local project docs and rules as the main product context source
